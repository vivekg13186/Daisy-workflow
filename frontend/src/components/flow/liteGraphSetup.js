// DEPRECATED — kept as an empty placeholder.
//
// The flow canvas was rewritten to use VueFlow (see CanvasTab.vue,
// NodeRegistry.js, nodes/PluginNode.vue, nodes/PluginPropertyPanel.vue).
// The previous litegraph.js implementation that lived here has been removed
// and `litegraph.js` is no longer a dependency. This file is intentionally
// blank so any stale import resolves to a harmless no-op until callers are
// updated. Safe to delete once nothing references it.
